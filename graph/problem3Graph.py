import graphs
#importing only the functions we need
from input import create_graph, choose

# we say that graph is of type graph from the graphs file
graph = graphs.graph

graph = create_graph()

choose(graph)